# 测试管理平台(TMS)后端 - 依赖清单
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pymysql>=1.1
python-dotenv>=1.0
requests>=2.31

# 通义千问 SDK(可选;未安装则走 OpenAI 兼容 HTTP 接口)
dashscope>=1.20

# Auto-Fix Agent(OpenAI 兼容模式调通义千问 function calling)
openai>=1.30

# Agent 工程韧性:LLM 调用重试(指数退避) + 补丁写文件的跨进程互斥锁
# 安装: pip install tenacity filelock
tenacity>=8.2
filelock>=3.13

# RAG 记忆:本地向量库(修复案例 embedding 存取,Few-shot 检索)
# 安装: pip install chromadb   (纯本地库,无需服务端;首次 import 会下载默认中文向量模型)
chromadb>=0.5
